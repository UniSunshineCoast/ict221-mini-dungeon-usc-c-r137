package dungeon.engine;

public class Entry extends Tiles{

    /**
     * Initializes the position of the Entry and its tile type
     *
     */
    public Entry(int x, int y) {
        tileX = x;
        tileY = y;
        tileType = 7;
    }
}
